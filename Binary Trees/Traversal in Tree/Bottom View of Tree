// in this question we using the TREEMAP
// TREEMAP -> Sort the Hashmap as per the keys
// here just the conspet of x axis like
// root -> 0, root.left -> -1, root.right -> +1


class Solution {
    class pair <T,U>{
        T first;
        U second;
        
         pair (T first, U second){
            this.first= first;
            this .second = se cond;
        }
    }
    public ArrayList<Integer> bottomView(Node root) {
        ArrayList<Integer> ans = new ArrayList<>();
        Map<Integer, Integer> mp = new TreeMap<>();
        Queue<pair<Node,Integer>> q = new LinkedList<>();
        
        q.add(new pair(root,0));
        
        while (!q.isEmpty()){
            
            pair<Node,Integer> curr = q.poll();
            Node temp = curr.first;
            int line = curr.second;
            
            mp.put(line, temp.data);
            
            if (temp.left != null) q.add (new pair (temp.left , line -1));
            if (temp.right != null) q.add(new pair (temp.right, line+1));
        }
        for (Map.Entry<Integer,Integer> entry : mp.entrySet()){
            ans.add(entry.getValue());
        }
        return ans ;
    }
}